/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // experimental: {
  //   esmExternals: true,
  // },
  output: "standalone",
};

export default nextConfig;
